__pycache__/
*.py[cod]
*.pyo
.env
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
uploads/
*.log

# Persistent AI-response cache (machine-local)
.cache/

# OMR working data (downscaled pages, oemer MusicXML, logs)
_omr/

# Glyph library user data (collected/unidentified glyphs + images)
app/data/

# Embedded source PDFs — large binaries kept locally for relationships, not in git.
# The registry (app/core/sources.json) and page renders ARE version-controlled.
app/core/sources/*.pdf
